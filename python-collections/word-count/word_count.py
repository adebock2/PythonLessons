
from collections import Counter
arrWords{}
def count_words(sentence):
   sentence = sentence.lower()
   lstWords = sentence.split(":"," ", ".","?", "!")
   CntWords = Counter.lstWords
   n = 0
   
   for n in CntWords:
    lstWords(n)
